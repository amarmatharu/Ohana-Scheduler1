# Project registry (public npm). Helps npm ignore corp Nexus entries from a parent
# or global ~/.npmrc when installing from this folder.
registry=https://registry.npmjs.org/
# Align with yarn.lock-era tree (react-day-picker vs date-fns peer).
legacy-peer-deps=true
